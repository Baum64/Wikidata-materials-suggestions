# Laufzeit-Abhaengigkeiten. Benoetigt Python >= 3.10 (PEP-604-Syntax
# "str | None" in wikikg/cli.py).

# wikikg, nomadwiki, benchmark
requests>=2.34

# zusaetzlich fuer "Kategorie Hirachie/" (Graphaufbau + PNG-Ausgabe)
networkx>=3.3
matplotlib>=3.9

# nur fuer die Tests
pytest>=9.0
